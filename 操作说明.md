# 📺 IPTV 手动测速与推送操作指南 (PowerShell 版)

本指南用于帮助你在本地电脑上使用 **PowerShell** 进行 IPTV 直播源的真实网络测速、剔除卡顿/死链，并将最新的播放列表自动同步推送至 GitHub 仓库。

---

## 🔗 你的订阅链接

在播放器（如 Tivimate、IPTV Smarters、NT Player 等）中输入的订阅地址（推荐以下两种防缓存链接）：

**推荐主力（国内高速无缓存，即推即见）：**
```text
https://ghproxy.net/https://raw.githubusercontent.com/SimonTGR/IPTV_Worker/main/output/user_result.m3u
```

**CDN 加速源（如果使用 jsdmirror，建议带时间戳或版本参数避免 CDN 节点缓存旧文件）：**
```text
https://cdn.jsdmirror.com/gh/SimonTGR/IPTV_Worker@main/output/user_result.m3u?v=20260907
```
或 jsdelivr 官方加速：
```text
https://fastly.jsdelivr.net/gh/SimonTGR/IPTV_Worker@main/output/user_result.m3u
```

---

## 🚀 PowerShell 手动测速与推送完整步骤

### 第一步：进入项目目录
在 **PowerShell** 窗口中直接运行：
```powershell
cd E:\Simon\IPTV
```

---

### 第二步：拉取云端最新代码
在每次测速前，建议同步一下远程仓库：
```powershell
git pull origin main
```

---

### 第三步：运行本地真实网络测速
在 PowerShell 中运行以下命令开始测速：
```powershell
pipenv run python main.py
```
> 💡 **说明**：
> - 本地测速耗时约 5 ~ 8 分钟（包含全量并发测速、广告识别、流媒体校验）。
> - 程序会根据配置自动为每个频道挑选最多前 **2** 个速度最快、最稳定的源。
> - 测速完成后，结果会自动写入 `output/user_result.m3u`。

---

### 第四步：推送更新至 GitHub
测速完成后，依次运行以下三行 Git 命令：

```powershell
git add -f output/ public_output/
git commit -m "update: 本地真实网络精准测速推送"
git push origin main
```
> 🎉 **推送完成后**：重新刷新你的播放器订阅，即可同步享受最新测好的高画质低延迟直播源！

---

## 📋 常见问题与逻辑说明

1. **为什么云端 (GitHub Actions) 测速和本地不同？**
   - GitHub 属于海外服务器，无法访问中国电信内网及家宽 IP，若在云端测速会导致源被误判为 0 速率。
   - 因此 GitHub Actions 仅做云端自动合并；真正的精准测速需在你的本地电脑运行。

2. **为什么部分频道在 M3U 中只有 1 个链接？**
   - 配置文件中 `urls_limit = 2` 代表每个频道**最多保留 2 个**有效源。
   - 如果某个频道经过测速与过滤后，仅有 1 个源达标通畅，程序会如实仅保留该高质量源，避免保留卡顿死链。
